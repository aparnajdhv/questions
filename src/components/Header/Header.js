import React from 'react';
import style from '@/components/Header/Header.module.css';

export const Header = () => {
  return (
    <div className={`bg-primary text-center text-white ${style.header}`}>Questions</div>
  )
}
